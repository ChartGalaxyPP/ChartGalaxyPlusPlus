# Example

This synthetic training example is copied from the released PNG and annotation JSON. It illustrates the format and is not an independent quality measurement. `record.json` contains its identity; the scene graph and spatial list are also provided as separate JSON files.
